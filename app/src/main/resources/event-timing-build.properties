application.name=${project.artifactId}
application.version=${project.version}
build.revision=${git.commit.id.full}
build.sourceRef=${eventTiming.sourceRef}
build.origin=${eventTiming.buildOrigin}
build.dirty=${git.dirty}
