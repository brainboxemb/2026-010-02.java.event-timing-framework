package io.github.brainboxemb.eventtiming.domain;

/** Temporary bootstrap marker for the domain package inside the framework library. */
public final class DomainLayer {
    private DomainLayer() {
    }

    public static String name() {
        return "domain";
    }
}
