package io.github.brainboxemb.eventtiming.application;

/**
 * Minimal Waypoint lifecycle represented by the first executable status contract.
 *
 * <p>Operational lifecycle states are deliberately deferred until the corresponding SIP
 * increment introduces real Waypoint behaviour.</p>
 */
public enum WaypointLifecycle {
    CLOSED
}
