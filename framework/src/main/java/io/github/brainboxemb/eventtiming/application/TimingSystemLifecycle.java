package io.github.brainboxemb.eventtiming.application;

/**
 * Minimal timing-system lifecycle represented by the first executable status contract.
 *
 * <p>Operational lifecycle states are deliberately deferred until the corresponding SIP
 * increment introduces real timing-system behaviour.</p>
 */
public enum TimingSystemLifecycle {
    CLOSED
}
